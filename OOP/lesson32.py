"""
Менеджеры контекстов. Оператор with
-----------------------------------

Не обязательно прописывать блок finally для закрытия файлов. Да, и файловый менеджер контекста делает
это автоматически вне зависимости от возникновения возможных исключений.
Как же в деталях это происходит? Для этого мы вначале посмотрим, как вообще создаются свои собственные менеджеры контекстов.

В целом менеджер контекста – это класс, в котором реализованы два магических метода:
__enter__() и __exit__()

Когда происходит создание менеджера контекста с помощью оператора with, то автоматически вызывается метод класса __enter__.
А когда менеджер контекста завершает свою работу (программа внутри него выполнилась или произошло исключение),
то вызывается метод __exit__. Как видите, все предельно просто. И, как вы догадались,
в файловом менеджере происходит закрытие файлового потока именно в методе __exit__.

Далее, общий синтаксис вызова менеджера, следующий:
with <менеджер контекста> as <переменная>:
      список конструкций языка Python
Здесь «переменная» - это ссылка на экземпляр менеджера контекста, через которую, мы потом с ним можем работать.
При необходимости ее можно опустить и записать все вот в таком виде:
with <менеджер контекста>:
      список конструкций языка Python
Но тогда мы не сможем обратиться к объекту менеджера контекста.

Давайте создадим свой класс менеджера, который бы контролировал работу при изменении списка: если программа
в теле менеджера приводит к исключению (ошибке), то список должен оставаться прежним (без изменений)

Класс DefenderVector
Мы здесь в приватном свойстве сохраняем ссылку на вектор, который следует «защитить».
Далее, в методе enter создаем копию этого вектора и возвращаем его. То есть, переменная dv будет ссылаться на эту копию
и обработка внутри менеджера будет происходить с элементами этой копии, а не исходным вектором.
Затем, в методе exit мы проверяем: если исключений не произошло, то заменяем все элементы вектора __v
на преобразованные __temp. В результате, при выходе из менеджера, мы получим измененный вектор v1.
Если же было какое-либо исключение, то запись новых данных выполняться не будет и у нас останется прежний вектор v1.

Метод exit у нас возвращает значение False, что означает обработку исключения (если оно произошло) вышестоящим блоком.
Обычно именно так и делают, чтобы не скрывать возможные ошибки и в обработчике верхнего уровня реагировать
должным образом на ошибочные ситуации. Кстати, оператор return можно вовсе опустить, тогда метод exit возвратит None,
а оно интерпретируется как False. Так что, часто его не пишут.
Давайте для примера возвратим значение True и смотрите, при возникновении исключения, оно было перехвачено
менеджером и далее уже не распространялось. Снова вернем False, запустим и теперь видим это исключение снова.

Если бы мы убрали контекстный менеджер и просто в блоке трай эксепт оставили только цикл фор,
то список был бы частично изменен и в блоке эксепт поймали бы ошибку


При необходимости, менеджеры контекстов можно вкладывать друг в друга.
Например, при работе с файлами, можно выполнить такое вложение:
try:
    with open("myfile.txt") as fin:
        with open("out.txt", "w") as fout:
            for line in fin:
                fout.write(line)
except Exception as e:
    print(e)
Работает все очевидным образом. Сначала завершается (отрабатывает) вложенный менеджер, а затем, внешний (первый).
Во всем остальном логика работы такого вложения сохраняется.
------------------------------------------------------------------------------------------------------------------------

Стек — это фундамент исполнения кода
Для процессора и интерпретатора Python весь твой код — это набор инструкций, который выполняется в определенном контексте.
Основной уровень (Module Level): Когда ты просто запускаешь файл main.py, Python создает самый первый,
базовый «фрейм» (тарелку в стопке). Это контекст твоего модуля. Все переменные, которые ты создал (v1, v2),
лежат в этой первой тарелке.
Вход в with: Когда выполнение доходит до with DefenderVector(v1), Python вызывает «под капотом» метод __enter__.
В этот момент поверх «тарелки модуля» кладется «тарелка метода __enter__».
Ошибка: Когда внутри цикла случается IndexError, Python начинает искать ближайший блок try-except.
Он смотрит в текущую «тарелку» (где идет цикл). Если там нет обработки, он «выкидывает»
эту тарелку и переходит к той, что ниже.

Как исключение «летит» по стеку
Представь это как эстафету, только бегут не вперед, а назад:
Ошибка возникла на строке dv[i] += v2[i].
Python спрашивает: «Здесь есть except?» — Нет.
Python выходит из блока with и вызывает __exit__.
Python спрашивает у __exit__: «Ты обработал ошибку?» (это решается через return True/False).
Если return False (как у тебя), Python говорит: «Окей, я иду искать дальше».
Он выходит на уровень выше — туда, где у тебя написан try...except.
Там он находит совпадение (except IndexError) и наконец-то останавливается, выполняя print("error").
Если бы ты не написал try...except снаружи, ошибка пролетела бы сквозь всю твою программу (весь стек),
и Python просто выключил бы её, показав красные надписи в консоли.
------------------------------------------------------------------------------------------------------------------------

Стек против Кучи: Выделение памяти
Когда операционная система запускает твою программу (процесс), она выделяет ей виртуальное адресное пространство.
Стек и Куча (Heap) — это разные области в этой памяти, и устроены они по-разному.
Стек (Stack): Память под него выделяется сразу при создании потока (thread).
У него фиксированный размер (обычно несколько мегабайт). Это очень быстрая память, потому что процессор работает
с ней напрямую через специальный регистр — указатель стека (Stack Pointer).
Куча (Heap): Это огромная область памяти. Она не выделяется вся сразу. Программа запрашивает
у ОС кусочки кучи по мере необходимости (например, когда ты создаешь большой список или объект в Python).
Как они "растут"?
В классической архитектуре памяти Стек и Куча расположены в разных концах
адресного пространства и растут навстречу друг другу:
Стек обычно растет вниз (от старших адресов к младшим). Когда ты вызываешь функцию,
указатель стека просто смещается ниже.
Куча растет вверх.
Если они встретятся, произойдет катастрофа.
Но в современных системах между ними такое огромное расстояние, что это почти невозможно.
Особенность Python
Тут есть важный нюанс для тебя как для Python-разработчика:
В языках типа C++ в стеке лежат сами данные (числа, структуры).
В Python абсолютно всё — это объекты в Куче. В стеке Python хранит только ссылки на эти объекты.
Когда ты вызываешь функцию, Python кладет в стек новый «фрейм». В этом фрейме лежат адреса (указатели),
которые ведут в Кучу, где и хранятся твои реальные данные (списки, строки, числа).

Стек: Правила диктует ОС и процесс
Когда ты запускаешь любую программу (будь то скомпилированный файл на C++ или интерпретатор Python),
операционная система создает для нее процесс.
Заранее заданный размер: Действительно, размер стека для главного потока программы обычно жестко задан
в настройках ОС или заголовке исполняемого файла. В Linux это часто 8 МБ, в Windows — 1 МБ.
Зачем так делать? Стек должен быть невероятно быстрым. Чтобы достичь этой скорости,
ОС выделяет под него непрерывный кусок адресного пространства. Если бы стек рос хаотично,
процессору приходилось бы постоянно пересчитывать адреса, что замедлило бы работу.

Кто управляет? В Python ты можешь даже сам посмотреть или изменить этот лимит через модуль sys
(функция setrecursionlimit), хотя это меняет логический лимит интерпретатора, а не физический размер области памяти в ОС.

🌊 Куча: Динамика в реальном времени
С кучей всё иначе. Когда интерпретатор Python читает твой код, он не знает заранее,
сколько данных ты введешь или сколько объектов создашь.
В ходе выполнения (Runtime): Память в куче выделяется не в момент "компиляции" в байт-код, а именно тогда,
когда выполняется инструкция создания объекта. Например, когда срабатывает строка self.__temp = self.__v[:].
Запрос к ОС: Интерпретатор Python (написанный на языке C) вызывает системные функции (вроде malloc),
говоря операционной системе: «Мне нужно еще 500 КБ под новый список, найди мне свободное место в куче».
Менеджер памяти Python: Чтобы не дергать ОС по каждому пустяку, у Python есть свой внутренний "завхоз".
Он забирает у ОС большой кусок памяти и сам распределяет его под мелкие объекты (строки, числа).

🔄 Процесс компиляции vs Выполнение
Важно разделять:
Компиляция в байт-код: Python превращает твой текст в набор инструкций (.pyc файлы).
На этом этапе память под объекты еще не выделяется, просто создается "план действий".
Выполнение (PVM — Python Virtual Machine): Вот тут и начинается магия. PVM читает план и говорит:
«Так, тут создаем список — выделяем память в куче», «Тут вызываем метод __enter__ — кладем новый фрейм в стек».
------------------------------------------------------------------------------------------------------------------------

Вместо того чтобы вручную прописывать класс с методами __enter__ и __exit__, как мы делали с DefenderVector,
декоратор @contextmanager позволяет превратить обычную функцию-генератор в полноценный менеджер контекста.

Как это работает?
Внутри такой функции мы используем ключевое слово yield. Все, что идет до yield, — это логика __enter__.
Все, что после, — это логика __exit__.
Вот как выглядел бы твой защитник вектора через этот инструмент:
from contextlib import contextmanager
@contextmanager
def defender_vector(v):
    # Эта часть как __enter__
    temp = v[:]
    try:
        yield temp  # "Отдаем" временный список в блок with .. as ..
        # Если ошибок не было, фиксируем изменения (как в твоем __exit__)
        v[:] = temp
    except Exception as e:
        # Если была ошибка, мы можем ее обработать или пропустить
        print(f"Откат изменений из-за: {e}")
        raise # Пробрасываем ошибку дальше по стеку

Давай разберем, что происходит в случае с @contextmanager и простым yield без блоков try...except внутри самой функции:
Прерывание: Ошибка возникает в теле with.
Передача в генератор: Python «забрасывает» это исключение обратно в точку, где стоял yield.
Крах функции: Если внутри функции-менеджера нет try...except вокруг yield, исключение просто «взрывает»
функцию в этой точке. Код, который идет после yield, не выполняется.
Распространение: Ошибка летит дальше вверх по стеку к вызывающему коду.
Именно поэтому при использовании @contextmanager критически важно использовать блок try...finally.

Безопасный шаблон с finally
Чтобы гарантировать выполнение «заметания следов» (например, закрытие файла или откат транзакции), мы пишем так:
@contextmanager
def safe_resource():
    print("Открываем ресурс") # Аналог __enter__
    try:
        yield "ДАННЫЕ"
    finally:
        # Этот код выполнится ДАЖЕ если в блоке with была ошибка
        print("Закрываем ресурс в любом случае") # Аналог __exit__

В твоем примере с вектором, если мы хотим сделать «откат» при ошибке, нам нужно поймать её внутри функции:
@contextmanager
def defender_vector_managed(v):
    temp = v[:]
    try:
        yield temp
        v[:] = temp  # Выполнится только если ошибок НЕ БЫЛО
    except Exception:
        print("Откат!")
        raise # Пробрасываем ошибку дальше, чтобы внешний код знал о ней
В хорошей архитектуре действует принцип: «Лови ошибку там, где ты знаешь, что с ней делать».
Менеджер контекста отвечает только за атомарность (все или ничего). Его задача — откатить изменения.
Он не знает, нужно ли всей программе работать дальше или нет.
Вызывающий код (например, твой эндпоинт в FastAPI) должен решить: «Если данные не сохранились,
я должен вернуть пользователю 400 ошибку или попробовать другую операцию?».
Если мы уберем raise из менеджера, мы лишим вызывающий код возможности принять это решение.

"""

# fp = None
# try:
#     with open("myfile.txt") as f:
#         for t in f:
#             print(t)
# except Exception as e:
#     print(e)

class DefenderVector:
    """
    Данный класс это паттерн Транзакция
    Его задача: либо применить все изменения к списку целиком,
    либо (если возникла ошибка) не менять исходный список вообще.
    """
    def __init__(self, v):
        self.__v = v

    def __enter__(self):
        self.__temp = self.__v[:] # либо же через deepcopy
        return self.__temp

    def __exit__(self, exc_type, exc_val, exc_tb):
        """
        Почему именно self.__v[:] = ...?
        Если написать self.__v = self.__temp, ты просто переназначишь локальную
        переменную внутри класса на новый объект.
        Оригинальный список v1, который лежит "снаружи", не изменится.
        Запись self.__v[:] = ... (assignment to a slice) — это изменение содержимого существующего
        объекта по той же самой ссылке. Это гарантирует, что внешняя переменная v1 "увидит" изменения.
        :param exc_type: Тип исключения (например, ValueError, TypeError), если в блоке with произошла ошибка.
        Если ошибок нет, значение равно None.
        :param exc_val: Значение (экземпляр) исключения, содержащее сообщение об ошибке (обычно текст сообщения).
        Если ошибок нет, равно None.
        :param exc_tb: Объект трассировки (traceback), указывающий, в какой строке кода произошла ошибка.
        Если ошибок нет, равно None.
        :return:
        """
        if exc_type is None:
            self.__v[:] = self.__temp
        return False


v1 = [1, 2, 3]
v2 = [2, 3]
try:
    with DefenderVector(v1) as dv:
        for i, a in enumerate(dv):
            dv[i] += v2[i]
except IndexError:
    print("error")
print(v1)