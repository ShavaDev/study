"""
Введение в Python Data Classes
------------------------------

На этом занятии мы познакомимся с относительно новой идеей «быстрого» описания классов (классов данных – Data Classes).
Смотрите, довольно часто при объявлении классов в программах на Python приходится прописывать инициализатор.
То есть, передается несколько полей: name, weight, price, которые предполагается сохранять в объектах класса Thing.
И это довольно типовая ситуация. Мало того, если создать объект этого класса и вывести его в консоль,
то увидим нечто неинформативное, вроде:
<__main__.Thing object at 0x0000014B7D29A7D0>
Поэтому, опять же нередко, в таких классах с данными приходится прописывать магический метод __repr__().

Видите, сколько типовой писанины приходится делать для описания классов, подобных классу Thing?!
И это лишь необходимый минимум. Представьте объем рутиной работы для описания всего лишь нескольких таких классов.

Так как программист, зачастую, существо глубоко ленивое, но творческое, уже давно предпринимались
попытки автоматизировать этот процесс. И вот, начиная с версии Python 3.7, появилась возможность «из коробки»
использовать инструмент для оптимизации объявления классов, содержащих произвольные данные, то есть,
Data Classes. Для этого, в самом простом варианте, достаточно импортировать специальный декоратор,
который так и называется dataclass

И с его помощью объявим в программе еще один класс ThingData, который будет эквивалентен ранее объявленному классу Thing.

Смотрите, какое у нас получилось компактное объявление класса ThingData! Поясню, что здесь к чему.
Вначале должен идти декоратор dataclass, который будет формировать описание класса ThingData с
инициализатором и магическим методом __repr__(). Внутри класса следует описание параметров для инициализатора.
Да, все приведенные атрибуты внутри класса ThingData впоследствии, с помощью декоратора dataclass,
преобразуются в набор параметров инициализатора. При этом атрибуты обязательно должны быть аннотированы
соответствующими типами данных.

Давайте детальнее посмотрим, что в итоге будет содержать класс ThingData.
pprint(ThingData.__dict__)

Видим, что __dict__ ссылается на объект типа mappingproxy, внутри которого имеется инициализатор
__init__() и магический метод __repr__(). То есть, в класс ThingData автоматически были добавлены эти два метода.
Кроме того, имеется магический метод __eq__() для выполнения операции сравнения на равенство объектов класса ThingData.
Также имеется специальный словарь __dataclass_fields__, содержащий указанные нами атрибуты при описании класса ThingData.
Таким образом, класс ThingData обладает необходимым базовым функционалом при работе с атрибутами: name, weight и price.

Давайте в этом убедимся. Создадим объект этого класса:
td = ThingData("Учебник по Python", 100, 1024)
Обратите внимание, в качестве первого аргумента я указал строку, а вторые два – это вес и цена.
То есть, параметры в инициализаторе следуют в том же порядке, что и атрибуты, указанные при описании класса ThingData.
И это чаще всего так. Порядок, как правило, не изменяют и оставляют по умолчанию.

Далее, выведем на экран объект td с помощью функции print():
print(td)
Увидим в консоли информацию:
ThingData(name='Учебник по Python', weight=100, price=1024)
Видите, как информативно отображается объект td класса ThingData?!
Это, как раз, благодаря переопределению магического метода __repr__() внутри класса ThingData. Для уверенности,
мы можем даже явно его вызвать в функции print() следующим образом:
print(repr(td))
и увидим тот же самый результат. И это очень здорово! Нам теперь не нужно тратить время на написание типового,
рутинного кода. Да и само объявление класса в программе, чисто визуально, выглядит куда лучше.
Главное, правильно понимать это объявление.

Давайте попробуем протестировать еще один магический метод __eq__(), который также был добавлен в класс ThingData.
Увидим ожидаемое значение False. Но, если создать еще один экземпляр с тем же набором данных, то операция сравнения
вернет значение True.
Я, думаю, из этих примеров вы уже поняли, как работает переопределенный магический метод  __eq__()?
Он перебирает значение указанных нами атрибутов (name, weight, price) первого объекта и сравнивает
с соответствующими значениями этих же атрибутов второго объекта.
Это эквивалентно сравнению следующих кортежей на равенство:
(name, weight, price) == (name, weight, price)

Кстати, если нас это поведение не устраивает и нужно сравнивать, например, объекты только по весу (полю weight),
то, как вариант, никто не запрещает внутри класса ThingData объявить свой вариант магического метода __eq__()

В этом случае существующий метод (который был объявлен нами) не меняется декоратором dataclass.
И теперь сравнение происходит именно по весу

И так происходит работа с методами: __init__(), __repr__(), __eq__(). Если они существуют внутри класса, то остаются неизменными.

Следующий важный базовый момент, при определении Data Classes, состоит в порядке объявления
атрибутов с начальными значениями. Давайте предположим, что цена по умолчанию принимает значение 0.
Но здесь есть один маленький нюанс: все атрибуты со значениями по умолчанию должны идти последними.
Например, если поменять атрибуты weight и price местами, то интегрированная среда нам сразу подчеркнет атрибут price,
а при попытке выполнить программу появится ошибка. Почему так произошло?
И почему атрибуты с начальными значениями обязательно должны быть последними?
Ответ прост и я думаю, некоторые из вас уже догадались в чем дело. Так как эти атрибуты,
по сути, являются параметрами метода __init__() и идут в том порядке, в котором записаны в классе,
то при данной записи получаем следующее объявление инициализатора:
def __init__(self, name: str, price: float = 0, weight: int)
Видите в чем тут ошибка? Параметры со значениями по умолчанию должны идти последними,
а здесь это не так. Отсюда и возникает ошибка определения. Поэтому, когда мы объявляем класс как Data Class
с декоратором dataclass, то все атрибуты с начальными значениями должны быть записаны последними.
Второй тонкий момент со значениями по умолчанию связан с изменяемыми и неизменяемыми объектами.
Я напомню вам одно важное поведение функций и методов в языке Python. Предположим,
что мы в инициализатор класса Thing добавляем еще один параметр dims (размерность) следующим образом:
class Thing:
    def __init__(self, name, weight, price, dims=[]):
        self.name = name
        self.weight = weight
        self.price = price
        self.dims = dims
И, затем, создаем объект этого класса:
t = Thing("Учебник по Python", 100, 1024)
t.dims.append(10)
Если сейчас вывести список dims, то увидим вполне ожидаемый результат:
print(t.dims)
Но, если после этого создать еще один объект класса Thing, например, так:
t2 = Thing("Учебник по Python", 100, 1024)
и выведем у него список dims:
print(t2.dims)
то увидим не пустой список, а с одним уже существующим значением 10.
Чаще всего такое поведение приводит к непредвиденным ошибкам в работе программы,
т.к. начальный пустой список подразумевается независимым для каждого объекта класса Thing.
Тогда как в показанном примере он оказывается общим.

По этой причине при объявлении Data Classes атрибутам нельзя присваивать изменяемые объекты
в качестве значений по умолчанию. Например, следующее объявление приведет к ошибке:
@dataclass
class ThingData:
    name: str
    weight: int
    price: float = 0
    dims: list = []
Тогда спрашивается, как нам определить, например, список в качестве значения по умолчанию и так,
чтобы он был независимым для каждого объекта класса ThingData? Для этого следует воспользоваться
специальной функцией field() из модуля dataclasses, и определить через нее значение по умолчанию следующим образом:
dims: list = field(default_factory=list)
Здесь через параметр default_factory указывается, какой объект следует создавать
в момент вызова инициализатора класса ThingData и присваивать атрибуту dims. В данном примере – это объект list (список).
Теперь, при создании объекта этого класса:
td = ThingData("Учебник по Python", 100, 512)
print(td)
мы увидим, что dims ссылается на пустой список, как это и ожидалось.
------------------------------------------------------------------------------------------------------------------------

1. В чем именно заключается проблема?
Проблема возникает вот здесь:
class Club:
    # ОШИБКА: список как значение по умолчанию
    def __init__(self, members=[]):
        self.members = members
В Python значения по умолчанию для аргументов функции вычисляются всего один раз — в тот момент,
когда интерпретатор впервые видит определение класса (при запуске программы).

2. Почему это «ловушка»?
Когда ты создаешь список [] прямо в заголовке __init__, Python создает один-единственный объект-список
в памяти и «приклеивает» его к этому методу.
Теперь, сколько бы ты ни создавал экземпляров класса Club, все они будут по умолчанию ссылаться на один
и тот же список в памяти.

3. Почему с числами (int) или строками (str) это не страшно?
Ты можешь спокойно писать sides: int = 6, потому что числа, строки и кортежи — это неизменяемые (immutable) типы данных.
Когда ты меняешь число, Python не меняет старый объект, а просто создает новый.
А со списком он меняет содержимое того же самого объекта в памяти.

4. Как делать правильно (Стандарт Python)
Чтобы у каждого объекта был свой собственный пустой список, используется классический прием с None:
class Club:
    def __init__(self, members=None):
        # Если ничего не передали, создаем НОВЫЙ список специально для этого объекта
        if members is None:
            members = []
        self.members = members
------------------------------------------------------------------------------------------------------------------------
"""

from dataclasses import dataclass, field
from pprint import pprint

class Thing:
    def __init__(self, name, weight, price):
        self.name = name
        self.weight = weight
        self.price = price

    def __repr__(self):
        return f"{self.__class__.__name__}: {self.__dict__}"

    # def __str__(self):
    #     return f"{self.__class__.__name__}: {self.__dict__} 1111111111"


@dataclass
class ThingData:
    name: str
    weight: int
    price: float = 0
    dims: list = field(default_factory=list)

    # def __eq__(self, other):
    #     return self.weight == other.weight


# t = Thing("Учебник по Python", "100", "1024")
# print(t)
td = ThingData("Учебник по Python", 100, 512)
# print(td)
# pprint(ThingData.__dict__)
# td2 = ThingData("Python ООП", 80, 512)
# print(td == td2)
td.dims.append(10)
print(td)
td2 = ThingData("Python", 100, 512)
print(td2)