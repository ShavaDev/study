"""
Вложенные классы
----------------

На этом занятии речь пойдет о вложенных классах. В двух словах, это когда один класс объявлен внутри другого.
И здесь сразу возникает два главных вопроса: как это работает и зачем это надо?
На мой взгляд, хороший пример использования вложенных классов – это описание моделей во фреймворке Django.
Здесь объявляется некий класс Women, далее прописаны два атрибута, которые в Django являются ссылками на
определенные классы, но в нашем примере – это просто строки. Причем, атрибуты title и photo – это поля в таблице БД,
с которой впоследствии идет работа, то есть, полагаем, что нам важно называть эти атрибуты именно такими именами.
Затем, идет объявление вложенного класса Meta с одним атрибутом ordering. В Django с помощью такого класса Meta
определяют некие вспомогательные параметры. В частности, параметр ordering
определяет сортировку по полю id записей в таблице БД.
Но все же, зачем нужен такой вложенный класс? Почему бы этот атрибут не прописать непосредственно в классе Women?
Давайте предположим, что в нашей таблице есть поле с именем ordering и для работы с ним в классе Women его следует прописать.
Давайте предположим, что в нашей таблице есть поле с именем ordering и для работы с ним в классе
Women его следует прописать, сделаем это так:
class Women:
    title = 'объект класса для поля title'
    photo = 'объект класса для поля photo'
    ordering = 'объект класса для поля ordering'

    class Meta:
        ordering = ['id']
Видите, если бы не было вложенного класса Meta, то возникла бы проблема с использованием одного
и того же атрибута ordering для разных целей: и для представления данных поля ordering и для управления сортировкой.
Вложенный класс Meta, фактически, создает независимое пространство имен для разрешения подобных конфликтов.
Разумеется, мы можем совершенно спокойно обращаться ко всем атрибутам класса через основной класс Women.
То же самое можно делать и через экземпляры класса Women.
Причем внутри самого объекта у нас сейчас не будет никаких локальных свойств: w.__dict__ = {пусто}.
Давайте для примера добавим инициализатор в класс Women.
Теперь, при создании объектов, в них дополнительно будут создаваться локальные свойства user и psw
Но обратите внимание, объект класса Meta при этом не создается.
Здесь Meta – это, по сути, еще один атрибут (пространство имен) в классе Women и мы к нему обращаемся
через w как к атрибуту класса, не более того. Если нам нужен объект класса Meta,
его нужно явно создать в инициализаторе класса Women.
Теперь в экземпляре класса Women имеется ссылка meta на объект класса Meta.
Чтобы в этой операции был смысл, давайте пропишем инициализатор во вложенном классе.
Теперь, при создании объекта класса Women будут автоматически создаваться два объекта:
класса Women и класса Meta с соответствующими локальными свойствами.
И, обратите внимание, мы не можем из класса Meta или из его объектов обращаться к атрибутам класса Women.
Это совершенно изолированное пространство имен от внешнего класса. Например, запись вида:
    class Meta:
        ordering = ['id']
        t = Meta.title
приведет к ошибке, так как у вложенного пространства имен класса нет ссылки на внешнее пространство имен класса Women.
Мало того, непосредственно во вложенном классе мы даже не можем явно указывать класс Women,
то тоже приведет к ошибке, так как пространство имен Women на данный момент еще не создано.
А вот обращаться к пространству имен Women внутри инициализатора класса Meta можно.
Все дело в том, что, когда выполняется строчка t = Meta.title, то класс Women он просто не создан,
его пока еще нет, этого пространства имен не существует, и мы обращаемся вот здесь вот к несуществующему пространству имен.
А вот внутри __init__ можно уже сделать, потому что когда инициализатор срабатывает, то все классы созданы, они
существуют, и тогда мы можем через пространство имен __init__ обращаться к классу Women.

Но, обычно, вложенные классы содержат изолированный код, который доступен только из внешнего класса.
Именно так следует проектировать программы при использовании вложенных классов.

В заключение занятия отмечу, что подобные вложения служат исключительно для удобства программирования.
Какого-то особого, сакрального смысла во вложенных классах нет и все что можно делать через вложения,
также можно реализовать и без них, используя обычные, независимые объявления. Но вы вполне можете столкнуться
с ними в других проектах, в частности, как я отмечал вначале, во фреймворке Django.
------------------------------------------------------------------------------------------------------------------------

1. Как мы обращаемся к Meta в __init__ класса Women, если он «еще не существует»?
Тут кроется главный секрет. В Python код внутри тела класса (class Women: ...) выполняется сверху
вниз в момент импорта или запуска файла.

Вот как это видит Python:

Он заходит в class Women.

Создает методы __init__, но не выполняет их код прямо сейчас (он просто запоминает, что такой метод есть).

Доходит до строки class Meta. И вот тут — внимание — он полностью выполняет код класса Meta и создает объект-класс Meta.

Только после того, как Meta полностью создан, он становится атрибутом класса Women.

Класс Women завершает свое создание.

Итог: К тому моменту, когда ты вызываешь w = Women(...) и срабатывает __init__, класс Meta уже давно «родился»
и спокойно лежит внутри Women. Поэтому self.Meta(...) работает без проблем.

2. Почему внутри тела Meta нельзя обратиться к Women?
Ты пишешь: «почему нельзя до инициализации вложенного класса обращаться к классу Women? ведь инициализатор уже сработал».

Здесь важно не путать инициализатор объекта (__init__) и определение класса (class ...).

Когда Python находится внутри блока:
class Women:
    ...
    class Meta:
        t = Women.title  # ОШИБКА ЗДЕСЬ
В этот момент Python все еще строит класс Women. Имя Women еще не внесено в глобальную область видимости (Global Namespace),
потому что создание класса не завершено (оно завершится только на самой последней строчке/отступе внешнего класса).

Поэтому попытка вызвать Women.title внутри Meta (на уровне самого тела класса) — это попытка обратиться к объекту,
который еще «не достроился».

3. Почему внутри __init__ вложенного класса обращаться к Women уже можно?
Потому что __init__ срабатывает только тогда, когда ты создаешь экземпляр (пишешь w = Women(...)).

К этому моменту:

Класс Women уже полностью определен и существует в памяти.

Класс Meta уже полностью определен и существует в памяти.

Поэтому внутри любого метода (будь то __init__ или обычный def) ты можешь спокойно писать
Women.title — Python легко найдет этот класс в глобальной области видимости.
------------------------------------------------------------------------------------------------------------------------

1. Жизненный цикл Класса vs Объекта
Давай посмотрим на хронологию событий в памяти Python:

Этап А: Создание Классов (Происходит один раз при запуске файла)

Python читает код class Women: ....

Он создает в памяти объект-класс Women. В этот момент в глобальной области видимости (globals()) уже есть имя Women.

Все методы (__init__, Meta) уже упакованы внутрь этого объекта-класса.

Важно: На этом этапе __init__ еще не запускался! Но сам класс (как пространство имен с атрибутами title, photo)
уже полностью готов.

Этап Б: Создание Экземпляра (Когда ты пишешь w = Women("root", "12345"))

Вызывается Women.__init__.

Внутри него ты пишешь self.meta = self.Meta(...).

В этот момент вызывается Meta.__init__.

2. Почему Meta.__init__ видит Women?
Когда срабатывает Meta.__init__, ему абсолютно все равно, доработал ли __init__ у класса Women.
Ему важно только одно: существует ли в глобальном пространстве имен объект с названием Women?

А как мы выяснили на Этапе А — он существует!

Ты можешь обращаться к Women.title (атрибуту класса), потому что сам класс уже создан.
Тебе не нужно ждать завершения инициализации конкретного объекта w, чтобы прочитать данные из класса Women.
------------------------------------------------------------------------------------------------------------------------

В Python вложенность классов — это чисто косметическая штука для организации кода (пространства имен).
Вложенный класс Meta — это «сосед», который живет в гостях у Women, но у него нет автоматического ключа
от комнат (атрибутов) хозяина.

Отсутствие связи: Когда ты создаешь объект meta = Meta(...), этот объект знает только про свой self (свои атрибуты).
Он не знает, какой именно экземпляр Women его создал.

Где искать? Если ты внутри метода класса Meta напишешь print(user), Python начнет искать переменную user:

Сначала в локальном методе.

Потом в атрибутах самого объекта Meta.

Потом в глобальной области видимости.

Он никогда не заглянет «вверх» в объект Women сам по себе.
------------------------------------------------------------

Чтобы Meta мог вытащить атрибуты экземпляра Women, тебе нужно явно «познакомить» их.
Передать ссылку на "родителя" (Явный)
class Women:
    def __init__(self, user, psw):
        self.user = user
        # Передаем САМ объект (self) внутрь Meta
        self.meta = self.Meta(self)

    class Meta:
        def __init__(self, outer_instance):
            # Сохраняем ссылку на объект Women
            self.outer = outer_instance

        def get_parent_data(self):
            # Теперь мы можем достать что угодно!
            return f"Данные родителя: {self.outer.user}"

w = Women("root", "123")
print(w.meta.get_parent_data()) # Выведет: root


"""

class Women:
    title = "объект класса для поля title"
    photo = "объект класса для поля photo"
    ordering = "объект класса для поля ordering"

    def __init__(self, user, psw):
        self._user = user
        self._psw = psw
        self.meta = self.Meta(user + "@" + psw)

    # def get_meta(self):
    #     t = self.meta._access # работает потому что были выполнены инициализации для вложенного и внешнего класса
    #     return t

    class Meta:
        ordering = ["id"]
        # t = Women.title # error

        def __init__(self, access):
            self._access = access
            # print(f"Women._user: {Women._user}") # error
            # self._t = Women.title # обычно так лучше не стоит делать (хотя ошибки и не будет)

        # def get_women(self):
        #     t = Women.title
        #     return t


w = Women("root", "12345")
# print(w.ordering)
# print(w.Meta.ordering)
print(w.__dict__) # empty dict before __init__
print(w.meta.__dict__)
# print(w.meta.get_women())
# print(w.get_meta())



print("1. Начинаем создание классов...")

class Women:
    title = "Я - Атрибут Класса"

    class Meta:
        print("2. Python зашел внутрь определения Meta")
        # print(Women.title)  # ОШИБКА! Women еще не достроен

        def __init__(self):
            print("5. Работает Meta.__init__")
            print(f"Доступ к классу Women: {Women.title}") # РАБОТАЕТ!

    def __init__(self):
        print("4. Начался Women.__init__")
        self.meta = self.Meta()
        print("6. Women.__init__ закончен")

print("3. Классы полностью созданы и готовы к работе!")

w = Women()