"""
Пользовательские метаклассы. Параметр metaclass
-----------------------------------------------

В Python можно описывать свои собственные метаклассы, которые, конечно же,
явно или неявно наследуются от основного метакласса type.

Давайте предположим, что мы бы хотели создавать классы для точек, в которых бы автоматически появлялись
атрибуты MAX_COORD и MIN_COORD. Для этого мы создадим метакласс в виде функции
(да, обычные функции тоже можно использовать как метаклассы)

Обратите внимание, функция-метакласс должна иметь три параметра: name – имя создаваемого класса; base – кортеж
из базовых классов; attrs – словарь с атрибутами класса.

Сейчас в этой функции мы в словарь attrs просто добавляем два атрибута MAX_COORD и MIN_COORD,
а затем, явным вызовом метакласса type формируем новый класс и возвращаем его.
Теперь, чтобы эта функция использовалась в качестве метакласса, при объявлении класса нужно
прописать специальный параметр metaclass и передать ссылку на эту функцию

Создадим объект этого класса, обратимся к атрибуту MAX_COORD и вызовем метод get_coords.
Как видите, все сработало, как мы и ожидали. В нашем классе автоматически появились атрибуты MAX_COORD и MIN_COORD,
а также явно, привычным образом, прописали метод get_coords, что намного удобнее.

Фактически, здесь класс был создан в функции create_class_point, в которую сам язык
Python передал нужный набор аргументов с соответствующими значениями, а далее,
с помощью вызова объекта type был создан этот класс. Такой подход намного удобнее,
чем прописывать все атрибуты непосредственно в словаре attrs.

Однако, использование функции в качестве метакласса – это, скорее, учебный пример
для лучшего понимания сущности метаклассов. Все же, на практике чаще используют отдельные классы,
которые играют роль метаклассов.

Давайте повторим нашу реализацию но не через функцию, а через класс.
Вначале объявим класс с именем Meta (имя выбираем сами, это просто пример), который должен наследоваться от метакласса type

Внутри этого класса мы прописали инициализатор с четырьмя параметрами.
Первый cls – это ссылка на новый уже созданный класс, а три остальных вам уже знакомы.
Внутри инициализатора мы вызываем инициализатор базового класса, а затем, динамически добавляем два атрибута
MAX_COORD и MIN_COORD.

А далее все то же самое. Через параметр metaclass указываем метакласс для создания класса Point и тестируем его работу.

Однако, инициализатор __init__() в классе Meta вызывается когда класс Point полностью создан. Для более тонкой работы
лучше переопределить магический метод __new__, который вызывается непосредственно перед созданием класса.

Здесь записаны четыре параметра: cls – ссылка на текущий класс Meta; name_class – имя создаваемого класса;
base – кортеж из базовых классов; attrs – словарь атрибутов создаваемого класса.
Так как метод __new__ вызывается до создания нового класса, то мы добавляем новые атрибуты
MAX_COORD и MIN_COORD непосредственно в словарь attrs. А, затем, вызываем аналогичный метод __new__
у объекта-метакласса type. Обратите внимание, метод __new__ должен вернуть ссылку на созданный класс,
то есть, обязательно следует прописать оператор return.
После запуска программы видим, что класс Point создается в соответствии с алгоритмом метакласса Meta.

Возможно, здесь у вас возник вопрос, а в чем преимущество использования классов в качестве метаклассов перед функциями?
Принципиальных отличий здесь нет. Любой алгоритм можно прописать и на уровне функции и на уровне класса.
Однако, при использовании классов у нас в руках оказывается вся мощь ООП. В частности, можно создавать
иерархии для формирования сложного поведения метаклассов и в крупных проектах это заметно облегчает
код и делает его гораздо читабельнее. Поэтому на практике чаще все же используют классы из-за их удобства.

------------------------------------------------------------------------------------------------------------------------

В чем разница?
То, что ты написал (A = type(...)), — это прямое использование стандартного станка.
Ты не переделывал сам станок, ты просто подошел к нему и скормил ему чертеж вручную,
вместо того чтобы это сделал интерпретатор за кулисами.
Использование type(): Ты используешь стандартный механизм Python для сборки класса.
Это «динамическое создание», но по стандартным правилам.
Собственный метакласс: Ты модифицируешь сам станок. Ты создаешь сущность, которая меняет логику создания классов.

Как выглядит собственный метакласс
Чтобы создать метакласс, тебе нужно наследоваться от type. Метакласс — это «инструкция по сборке инструкций».
Обычно в нем переопределяют метод __new__ (который отвечает за создание объекта-класса).
Вот пример: допустим, мы хотим, чтобы в нашей ИТ-лаборатории все классы обязательно имели документацию (docstring).
Если программист ее не написал — класс не должен даже создаться.
# Наш собственный метакласс
class StrictMeta(type):
    def __new__(cls, name, bases, dct):
        # name - имя класса
        # bases - кортеж родительских классов
        # dct - словарь атрибутов и методов

        if "__doc__" not in dct or not dct["__doc__"]:
            raise TypeError(f"Бро, ты забыл написать документацию для класса {name}!")

        return super().__new__(cls, name, bases, dct)

# Теперь используем его
class MyGoodClass(metaclass=StrictMeta):
    ""Это очень полезный класс.""
    pass
# А вот это вызовет ошибку еще ДО создания каких-либо объектов
# class MyBadClass(metaclass=StrictMeta):
#     pass

Можно ли объединить твой пример и метакласс?
Да! И вот тут ты увидишь разницу. Помнишь свою строку A = type(...)? Первый аргумент там был type.
Если ты хочешь создать класс динамически, но используя свой метакласс, это будет выглядеть так:
# Динамическое создание через СВОЙ метакласс
A = StrictMeta("Point", (object,), {"__doc__": "Я документация!", "x": 10})

Почему это «динамическое создание»?
Потому что в обычном коде ты пишешь class MyClass: ... и это зашито намертво.
А в примере с A = StrictMeta(...) ты можешь сделать так:
# Представь, что данные пришли из API или конфига
class_name = "User"
class_attrs = {"name": "Ivan"} # Тут НЕТ документации!

# Пытаемся создать класс динамически
try:
    NewClass = StrictMeta(class_name, (object,), class_attrs)
except TypeError as e:
    print(f"Ошибка: {e}")
    # Выведет: Ошибка: Бро, ты забыл написать документацию для класса User!
------------------------------------------------------------------------------------------------------------------------

Этап 1: Создание метакласса Meta
Python читает код сверху вниз.

Видит class Meta(type):.

Создает в памяти объект-метакласс. Теперь Python знает: «У меня есть станок Meta, который умеет штамповать другие классы».

На этом этапе больше ничего не происходит. Никакие методы __new__ пока не вызываются.

Этап 2: Создание класса Point (Самый важный момент!)
Python идет дальше и видит: class Point(metaclass=Meta):.
Внимание: В этот момент Python еще не создал класс Point. Он только готовится.

Сбор данных: Python смотрит внутрь class Point и видит там метод get_coords. Он кладет его в «черновик» (словарь attrs).

Поиск конструктора: Python видит metaclass=Meta. Он понимает: «Так, я не буду создавать
этот класс сам через стандартный type, я позову Meta».

Вызов Meta.__new__: Именно сейчас, когда Python только прочитал определение класса Point, вызывается __new__ в метаклассе.

В этот __new__ передается тот самый «черновик» attrs (где пока только get_coords).

Внутри __new__ ты делаешь: attrs.update({"MAX_COORDINATES": 100}).

Теперь в «черновике» уже ТРИ вещи: get_coords, MAX_COORDINATES и MIN_COORDINATES.

Рождение класса: Выполняется type.__new__(...). Python берет дополненный черновик и официально регистрирует
в памяти класс Point.

Итог этапа 2: У тебя в памяти уже есть готовый класс Point, у которого в «кармане» (в атрибутах)
лежат и координаты, и метод.

Этап 3: Создание экземпляра pt = Point()
Вот здесь ты и запутался. Когда ты пишешь pt = Point():

Класс Point уже существует.

Он уже содержит MAX_COORDINATES (потому что метакласс добавил их еще на Этапе 2).

Python просто берет готовый шаблон Point и штампует по нему объект pt.

Ответ на твой главный вопрос: «Когда успевает добавиться атрибут?»
Атрибуты добавляются в момент объявления класса, а не в момент создания объекта pt.

"""

# class Point:
#     MAX_COORDINATES = 100
#     MIN_COORDINATES = 0


# создание метакласса через функцию
# def create_class_point(name, base, attrs) -> type:
#     attrs.update({"MAX_COORDINATES": 100, "MIN_COORDINATES": 0})
#     return type(name, base, attrs)


class Meta(type):
    def __new__(cls, name, bases, attrs): # cls здесь ссылка на сам класс Meta
        attrs.update({"MAX_COORDINATES": 100, "MIN_COORDINATES": 0})
        return type.__new__(cls, name, bases, attrs)

    # def __init__(cls, name, base, attrs, *args, **kwargs): # здесь cls это ссылка на новый уже созданный класс
    #     super(Meta, cls).__init__(name, base, attrs, *args, **kwargs)
    #     cls.MAX_COORDINATES = 100
    #     cls.MIN_COORDINATES = 0


class Point(metaclass=Meta): # metaclass=create_class_point
    def get_coords(self) -> tuple:
        # print(self.MAX_COORDINATES, self.MIN_COORDINATES)
        return (0, 0)


pt = Point()
print(pt.MAX_COORDINATES)
print(pt.get_coords())



print("Сейчас Питон начнет читать класс Мета")
class Meta(type):
    print("Внутри класса Мета")

    def __new__(cls, name, bases, attrs):
        print(f"--- 1. Метакласс Meta работает для класса {name} ---")
        attrs.update({"MAX_COORDINATES": 100})
        return super().__new__(cls, name, bases, attrs)

    print("Выхожу с класса Мета")

print("--- Сейчас Python начнет читать класс Point ---")

class Point(metaclass=Meta):
    print("Читаю класс Поинт")

    def get_coords(self): pass

    print("Выхожу с класса Поинт")

print("--- Класс Point уже создан! ---")

print("--- Сейчас создадим объект pt ---")
pt = Point()


