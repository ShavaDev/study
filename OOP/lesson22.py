"""
Функция super() и делегирование
-------------------------------

| Метод _call_ вызывается из метакласса type |

Сморите, если у нас имеется некий базовый класс Geom и мы создаем дочерний класс Line,
в котором дополнительно прописан метод draw(), то это называется расширением (extended) базового класса.

Как правило, дочерние создаются именно для расширения функциональности базовых классов.
Однако, если в классе Geom также прописать метод draw(), то теперь класс Line лишь переопределяет (overriding)
поведение базового класса, не меняя его принцип функционирования. Поэтому, когда говорят о расширении,
то подразумевают добавление новых атрибутов в дочерних классах,
а при переопределении (обычно методов) – изменение поведения уже существующего функционала.


Давайте теперь пропишем инициализатор в базовом классе Geom.
А ниже создадим экземпляр класса Line:
l = Line()
После запуска программы увидим ожидаемое поведение – был автоматически вызван инициализатор базового класса.
В действительности, здесь происходит следующая последовательность вызовов магических методов.
Сначала вызывается __call__(), который, в свою очередь, последовательно вызывает метод __new__()
для создания экземпляра класса, а затем, метод __init__() для его инициализации.
Так вот, все эти методы вызываются из дочернего класса Line. Если какой-либо из них не находится,
то поиск продолжается в родительских классах в порядке иерархии наследования. Например, метод __new__()
в данном случае будет взят из метакласса type, который неявно вызывается при создании классов
(подробнее о метаклассах мы еще будем говорить). А вот метод __init__() мы прописали в классе Geom,
поэтому будет вызван именно он. Причем, параметр self в этом методе будет ссылаться на созданный объект класса Line.
Об этом мы с вами уже говорили и это следует помнить. Параметр self в методах класса всегда ссылается на объект,
из которого метод был вызван.

Для класса Line был вызван инициализатор в базовом классе Geom, а для класса Rect не вызывался – только инициализатор
самого класса. И это логично, так как метод __init__() был найден в Rect и дальше цепочка поиска не продолжалась.
Но нам же нужно при создании примитивов также вызывать инициализатор и базового класса Geom. Как это сделать?
Конечно, мы могли бы явно указать имя базового класса Geom и вызвать через него магический метод __init__()
в инициализаторе класса Rect: Geom.__init__(self, x1, y1, x2, y2)
Но явно указывать имена базовых классов не лучшая практика, так как имена и иерархия наследования могут меняться.
Поэтому в Python для обращения к базовому классу используется специальная функция super().
Она возвращает ссылку на, так называемый, объект-посредник, через который происходит вызов методов базового класса.
Теперь, при запуске программы мы видим, что был вызван инициализатор сначала класса Geom, а затем, для Rect.
Такое обращение к переопределенным методам базового класса с помощью функции super() называется делегированием.
То есть, мы делегировали вызов инициализатора класса Geom, чтобы он создал в нашем объекте локальные свойства
с координатами углов прямоугольника. Причем, вызов метода __init__() базового класса лучше делать в первой же строчке,
чтобы он случайно не переопределял какие-либо локальные свойство в дочернем классе.
------------------------------------------------------------------------------------------------------------------------


1. Почему не нужен self?
Короткий ответ: super() — это умный прокси-объект.

Когда ты вызываешь super(), Python делает за тебя «грязную работу»: он находит родительский класс и
автоматически привязывает текущий экземпляр (self) к методам этого родителя.

Сравним старый и новый стиль:

Старый стиль (явный): Parent.__init__(self, name) — здесь мы обращаемся к классу напрямую как к функции,
поэтому обязаны передать self вручную, чтобы класс понимал, какой именно объект инициализировать.

Новый стиль (через super): super().__init__(name) — здесь super() уже «держит в руках» твой self.
Он сам подставляет его в родительский метод за кулисами.

2. Как super() работает «под капотом»
На самом деле super() — это не просто функция, а сокращенная запись. В Python 3, когда ты пишешь super(),
интерпретатор автоматически подставляет туда два аргумента:

Класс, в котором ты находишься.

Объект (self), с которым работаешь.

То есть super() — это то же самое, что super(Child, self).

Алгоритм работы:
Python смотрит на MRO (Method Resolution Order) — это список всех классов-предков в порядке их приоритета.

super() находит текущий класс в этом списке и берет следующий за ним класс.

Он берет твой self и «приклеивает» его к методу найденного класса.

3. Зачем вообще нужен super, если можно вызвать класс по имени?
Может показаться, что Parent.__init__(self) проще. Но super() критически важен в двух случаях:

А. Ромбовидное наследование (Diamond Problem)
Если у тебя сложная иерархия (Класс А -> Классы B и C -> Класс D), то без super()
базовый класс А может инициализироваться дважды, что приведет к ошибкам. super() гарантирует,
что каждый класс в цепочке будет вызван ровно один раз.

Б. Гибкость (Масштабируемость)
Если ты завтра решишь сменить родительский класс у Child, тебе придется переписывать имена во всех методах __init__.
С super() код адаптируется автоматически.

Итог:
super() — это автоматизация. Он избавляет тебя от необходимости вручную пробрасывать self.

Он работает на основе MRO, что делает его незаменимым в сложном наследовании.

В современных версиях Python 3 тебе не нужно передавать аргументы внутрь самого super(),
он достаточно умен, чтобы понять контекст.



Чтобы увидеть, как именно Python выстраивает очередь из твоих классов, используется атрибут __mro__ или метод mro().
Расшифровывается это как Method Resolution Order (Порядок разрешения методов).

1. Как посмотреть MRO?
Давай создадим «ромбовидную» структуру — это самый наглядный пример, где без super() всё ломается.

Python
class A:
    def __init__(self):
        print("Вход в A")
        print("Выход из A")

class B(A):
    def __init__(self):
        print("Вход в B")
        super().__init__()
        print("Выход из B")

class C(A):
    def __init__(self):
        print("Вход в C")
        super().__init__()
        print("Выход из C")

class D(B, C):
    def __init__(self):
        print("Вход в D")
        super().__init__()
        print("Выход из D")

# Смотрим порядок обхода классов
print(D.mro())
2. Что ты увидишь в консоли?
Список будет таким:
[D, B, C, A, object]

Почему это важно?
Когда ты вызываешь super().__init__() внутри класса B, Python не просто прыгает к его родителю (A).
Он смотрит в этот список mro и видит, что следующим идет класс C!

Именно поэтому, если ты запустишь d = D(), вывод будет таким:

Вход в D

Вход в B

Вход в C (хотя C — не родитель B, они «соседи»)

Вход в A

...и выход в обратном порядке.

3. Почему super() круче прямого вызова?
Если бы ты в классе B написал A.__init__(self) вместо super(), а в классе C тоже A.__init__(self),
то базовый класс A инициализировался бы дважды. А super() проходит по списку mro как по цепочке,
заходя в каждый класс ровно один раз.

4. Тонкий момент: super() с аргументами в Python 2 и 3
Как я уже говорил, в Python 3 мы пишем просто super(). Но полезно знать, что под капотом он превращается в:
super(ТекущийКласс, self)

Это буквально означает: «Возьми список MRO, найди в нем ТекущийКласс, перейди к следующему за ним и примени
его методы к объекту self».

Резюме:
MRO — это карта, по которой путешествует super().

Порядок в этой карте зависит от того, как ты перечислил классы при наследовании: class D(B, C) — сначала B, потом C.

super() всегда передает self дальше по цепочке незаметно для тебя.



Чтобы «сломать» Python, нужно создать ситуацию, когда порядок наследования логически противоречив.
Python использует алгоритм C3-линеаризации, который строго следит за тем, чтобы базовые классы не вызывались раньше своих наследников.

1. Как вызвать ошибку MRO?
Представь, что у нас есть два класса: A и B.
Мы пытаемся создать класс C, который наследуется от них обоих. Но при этом мы запутываем Python, говоря,
что в одном случае A важнее B, а в другом — наоборот.

Python
class A: pass
class B(A): pass  # B наследуется от A (B -> A)

# А теперь пытаемся создать "невозможный" класс:
try:
    class C(A, B): pass # Мы просим: сначала ищи в A, потом в его наследнике B
except TypeError as e:
    print(f"Ошибка: {e}")
Результат:
TypeError: Cannot create a consistent method resolution order (MRO) for bases A, B

2. Почему Python ругается?
Python соблюдает два правила:

Порядок в скобках: Если ты написал C(A, B), то A должен идти в очереди раньше B.

Порядок наследования: Но B является наследником A, значит по логике ООП, B более специфичен и должен идти перед A.

Возникает конфликт: ты просишь поставить родителя (A) перед ребенком (B), что нарушает иерархию «от частного к общему».

3. Как это лечится?
Лечение простое — нужно всегда ставить «детей» (более специфичные классы) перед «родителями» (базовыми классами) в списке наследования.

Python
# Правильно:
class C(B, A): pass

print(C.mro())
# [C, B, A, object] - логика сохранена!
Практический совет: как не запутаться в super()
Когда ты работаешь со сложным наследованием, всегда помни: super() — это не всегда «вызов родителя». Это «вызов следующего по списку MRO».

Если ты хочешь быстро проверить, куда полетит твой super(), просто выведи:
print(ТвойКласс.__mro__)

Это сэкономит тебе часы дебага в больших проектах.

"""

class Geom:
    name = "Geom"

    # def __init__(self):
    #     print("инициализатор Geom")

    def __init__(self, x1, y1, x2, y2):
        print(f"Инициализатор Geom для {self.__class__}")
        self.x1 = x1
        self.y1 = y1
        self.x2 = x2
        self.y2 = y2


class Line(Geom):
    # print("инициализатор Line")
    # def __init__(self, x1, y1, x2, y2):
    #     self.x1 = x1
    #     self.y1 = y1
    #     self.x2 = x2
    #     self.y2 = y2

    def draw(self):
        print("Рисуем линию")


class Rect(Geom):
    def __init__(self, x1, y1, x2, y2, fill=None):
        # Geom.__init__(self, x1, y1, x2, y2)
        # super(Rect, self).__init__(x1, y1, x2, y2) # то есть можно явно указать
        super().__init__(x1, y1, x2, y2)
        print("инициализатор Rect")
        # self.x1 = x1
        # self.y1 = y1
        # self.x2 = x2
        # self.y2 = y2
        self.fill = fill

    def draw(self):
        print("Рисуем прямоугольник")


l = Line(0, 0, 10, 20)
# print(l.__dict__)
r = Rect(1, 2, 3, 4)
print(r.__dict__)